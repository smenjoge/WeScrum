1. Updated <title> to 'Horiseon Home page'
2. Changed the first div in the body to <header> and last div to <footer>. Updated the style.css accordingly to refer to use tag selectors instead of class selectors. 
3. Changed <div> inside the 'content' and 'benefits' div to <section>
4. Fixed URL for 'Search Engine Optimization' by adding an id to first section inside class="content"
5. Added alt tags to all images.
6. alt tag is not added to the background image as it is a CSS property and not an HTML tag
7. Grouped same CSS properties together for multiple elements. 